{
    "name": "container",
    "version": "1.0.0",
    "type": "module",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node server.js",
        "docker:build": "docker.exe build -t oj-execution-service .",
        "docker:run": "docker.exe run --rm --name oj-execution-service -p 5001:5001 --cpus=1 --memory=1g --pids-limit=128 oj-execution-service"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "docker": "^1.0.0",
        "express": "^5.2.1",
        "uuid": "^14.0.0"
    }
}
